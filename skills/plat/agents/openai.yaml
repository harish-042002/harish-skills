interface:
  display_name: "Plat"
  short_description: "Evidence-led coding without unnecessary overhead"
  default_prompt: "Use Plat to make the smallest complete change, preserve every requirement, and verify the requested behavior. Keep ordinary tasks single-agent."
