interface:
  display_name: "Plat"
  short_description: "Backend-first engineering control plane for coding agents"
  default_prompt: "Use Plat to solve this engineering task: infer the workflow, load only relevant guidance, make the simplest correct change, and verify it with fresh evidence."
