interface:
  display_name: "Plat"
  short_description: "Minimum sufficient engineering depth for AI coding agents"
  default_prompt: "Use Plat to solve this engineering task with the minimum sufficient depth: inspect repository truth first, load only specialist guidance that can change the outcome, avoid unnecessary tool/context churn, course-correct on fresh evidence, and verify before claiming completion."
